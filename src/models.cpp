// models.cpp — explicit template instantiations / out-of-line defs.
//
// The RecommendationItem struct has no out-of-line members today; this
// translation unit exists so the build system has a stable object to
// link against and so future non-inline definitions have a home.
#include "models.hpp"

namespace marketly::recommendation {

// Intentionally empty — see header comment.

}  // namespace marketly::recommendation
